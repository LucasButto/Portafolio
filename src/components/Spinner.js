import React from "react";
import { SyncLoader } from "react-spinners";

const Spinner = () => {
  return (
    <>
      <SyncLoader color="#3e497a" margin={2} size={25} />
    </>
  );
};

export default Spinner;
